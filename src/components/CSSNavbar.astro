---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1); // remove the first "/"
---

<div class="flex bg-black text-white items-center">
	<div class="text-lg font-bold ml-4">Cannizzian Test</div>
	<div class="currentp">
		You are on the {currentPath === "" ? "home" : currentPath} page
	</div>
	<div class="ml-auto">
		<ul>
			<a class={currentPath === "" ? "currentp" : ""} href="/">Home</a>
			<a class={currentPath === "pieces" ? "currentp" : ""} href="/pieces"
				>Blog</a
			>
			<a class={currentPath === "about" ? "currentp" : ""} href="/about"
				>About Me</a
			>
		</ul>
	</div>
</div>

<style>
	ul {
		display: flex;
	}
	a {
		padding: 1rem;
	}
	a:hover {
		background-color: gray;
	}
	a:focus {
		outline: 0px;
		background-color: blue;
	}
	a:active {
		background-color: green;
	}
	.currentp {
		background-color: orange;
	}
</style>
